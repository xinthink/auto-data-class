apply plugin: 'kotlin-kapt'

dependencies {
    compile project(':lib')
    compile deps.gson
    compile deps.android.core

    kapt project(':processor')
    compileOnly project(':processor')

    kaptTest project(':processor')
    testCompileOnly project(':processor')
}
